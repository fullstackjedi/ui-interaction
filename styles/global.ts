const styles = {
  global: {
    // styles for the `body`
    body: {
      bg: '#fff',
      color: '#000'
    },
    // styles for the `a`
    a: {
      color: 'teal.500',
      _hover: {
        textDecoration: 'underline'
      }
    }
  }
};

export default styles;
